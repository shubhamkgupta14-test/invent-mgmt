`brand-logo.png` is the fallback logo.

Static files placed here are bundled with the frontend build.

Runtime uploads are served by the backend from its `UPLOAD_DIR` setting and loaded with `/uploads/...` URLs resolved against `VITE_API_BASE_URL`.
